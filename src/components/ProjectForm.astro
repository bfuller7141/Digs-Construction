<form id="project-request" class="form-wrap" action="/thank-you" method="POST" data-netlify="true" data-netlify-honeypot="bot-field" name="Project Request">
    <!-- Honey Pot (Spam Prevention) -->
    <div class="hidden" style="display: none;">
        <label for="bot-field">Don’t fill this out if you're human:</label>
        <input type="text" name="bot-field" id="bot-field" />
    </div>

    <!-- Subject Line -->
    <input type="hidden" id="subject" name="subject" value="New Project Request">

    <!-- Name Fields -->
    <div class="horizontal-group">
        <div class="form-group">
            <label for="first-name">First Name</label>
            <input type="text" name="first-name" id="first-name" placeholder="John" required autocomplete="given-name" />
        </div>
        <div class="form-group">
            <label for="last-name">Last Name</label>
            <input type="text" name="last-name" id="last-name" placeholder="Doe" required autocomplete="family-name" />
        </div>
    </div>

    <!-- Company Name -->
    <div class="form-group">
        <label for="company">Company Name</label>
        <input type="text" name="company" id="company" placeholder="Company Inc." required autocomplete="organization" />
    </div>

    <!-- Contact Fields -->
    <div class="horizontal-group">
        <div class="form-group">
            <label for="email">Email</label>
            <input type="email" name="email" id="email" placeholder="jdoe@email.com" required autocomplete="email" />
        </div>
        <div class="form-group">
            <label for="phone">Phone</label>
            <input type="tel" name="phone" id="phone" placeholder="505-555-5555" required autocomplete="tel" />
        </div>
    </div>

    <!-- Project Description -->
    <div class="form-group">
        <label for="project-description">Project Description</label>
        <textarea name="project-description" id="project-description" placeholder="Tell us about your project..." required></textarea>
    </div>

    <!-- Submit Button -->
    <div class="btn-wrapper">
        <button type="submit" class="btn-primary btn-form">Submit Request</button>
    </div>
</form>

<style>
    /* Form Wrapper */
.form-wrap {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
    width: 100%;
    max-width: 60rem;
    margin: var(--spacing-lg) auto;
}

/* Form Group */
.form-group {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
    width: 100%;
    box-sizing: border-box;
}

/* Horizontal Form Layout */
.horizontal-group {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    gap: var(--spacing-md);
}

/* Input Fields */
input, textarea {
    font-family: var(--font-body);
    font-size: var(--body-md);
    padding: var(--spacing-xs);
    border: 2px solid var(--gray-300);
    background-color: var(--white);
    border-radius: var(--border-radius);
    box-sizing: border-box;
    transition: border-color 0.2s ease-in-out;
    width: 100%;
}

/* Input Focus */
input:hover,
input:focus,
textarea:hover,
textarea:focus {
    outline: none;
    border-color: var(--red-500);
    transition: all 0.2s ease-in-out;
}

/* Button Wrapper */
.btn-wrapper {
    display: flex;
    justify-content: center;
    margin-top: var(--spacing-lg);
}

/* Responsive Adjustments */
@media (max-width: 768px) {
    .form-wrap {
        width: 100%;
    }
    .horizontal-group {
        flex-direction: column;
        gap: var(--spacing-md);
    }
}
</style>